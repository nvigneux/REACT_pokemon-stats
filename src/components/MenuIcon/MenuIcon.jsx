import React from "react"

import style from "./MenuIcon.module.css"

const MenuIcon = () => {
  return (
    <div className={`${style.btn} ${style.notActive}`}>
      <span />
      <span />
      <span />
    </div>
  )
}

export default MenuIcon
